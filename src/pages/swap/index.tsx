import SwapContainer from "../../components/Swap/SwapContainer";

function Swap() {

    return (
        <div className="w-full flex flex-col justify-center relative mt-[50px]">
            <SwapContainer />
        </div>
    );
}

export default Swap;
